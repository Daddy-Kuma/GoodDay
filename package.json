{
  "name": "worldcup-odds-pwa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check api/official-feed.js && node --check api/health.js",
    "health": "node -e \"fetch('http://127.0.0.1:'+(process.env.PORT||8081)+'/health').then(async r=>{console.log(await r.text());process.exit(r.ok?0:1)}).catch(e=>{console.error(e);process.exit(1)})\""
  }
}
